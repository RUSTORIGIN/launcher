# Build output
bin/
obj/
release/
*.msi
*.wixpdb

# Packaged client (multi-GB) and download temp - never commit
RustClient.zip
RustClient.zip.sha256
*.part
*.part.meta

# Loose built executables at the repo root
/RustOriginLauncher.exe
/RustOrigin.exe
/RustClient.exe

# Design scratch
.superdesign/tmp/

# Local logs
*.log

# Secrets / credentials - NEVER commit these
*.pem
*.key
*.pfx
*.p12
*.snk
*.env
.env
rclone.conf
**/secrets*
**/credentials*
# the manifest signing private key, if/when signing is added
*private*key*
signing-key*
